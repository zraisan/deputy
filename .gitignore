# secrets
.env
.env.local

# dependencies and build output
node_modules/
dist/
packages/extension/dist/

# demo capture intermediates — the encoded video is the artifact, not the frames
.demo-frames*/

# scratch
*.log

# landing page build inputs
site/node_modules/
site/dist/
